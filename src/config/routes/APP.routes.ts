import { KeyWithValue } from '@/common/types'

export const APPROUTES: KeyWithValue<string> = {
	LOGIN: '/',
	HOME: '/vision general',
	ORDERS: '/pedidos',
	PRODUCTS: '/productos',
	CLIENTS: '/clientes',
	SELLS: '/ventas'
}
